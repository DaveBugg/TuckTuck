// Константы настроек, нужные и серверу, и интерфейсу.
//
// Вынесены из settings.ts намеренно: тот модуль ходит в базу и читает файлы
// хоста, и притаскивать его в клиентский бандл ради одного массива с
// предустановками — верный способ получить node:fs в браузере.

export const RETENTION_PRESETS = [7, 30, 90, 180] as const;

// Верхняя граница не из вредности: при минутном интервале даже со свёрткой
// год истории на парк из двадцати машин — это заметный объём, а пользы от
// «нагрузка два года назад» нет никакой.
export const RETENTION_MIN = 1;
export const RETENTION_MAX = 730;
